# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

#
#GET     /tui                        controllers.HomeController.getTui()

GET     /menu                        controllers.HomeController.gameMenu()

GET     /playernumber/:id            controllers.HomeController.enterPlayerNumber(id: Int)

POST    /playernumber/:id            controllers.HomeController.enterPlayerNumber(id: Int)

GET     /playername/:id              controllers.HomeController.enterPlayerName(id: String)

#GET     /makemenu                   controllers.HomeController.makeMenu()

#GET     /wizard                     controllers.HomeController.showWizard()

#GET     /wizard/start               controllers.HomeController.showPlayerForm()

#GET     /ersteKarte                 controllers.HomeController.showCard()